{
  "name": "action-delete-record",
  "version": "1.0.0",
  "description": "Base automation action for deleting a record",
  "main": "index.js",
  "scripts": {
    "sync:appjson": "node ../../scripts/sync-basekit-app-json.js",
    "test": "pnpm run sync:appjson && ts-node test/index.ts",
    "build": "pnpm run sync:appjson && block-basekit-cli build",
    "preview": "pnpm run sync:appjson && block-basekit-cli preview",
    "upload": "pnpm run sync:appjson && block-basekit-cli upload"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@lark-opdev/block-basekit-cli": "^1.0.4-alpha.26"
  },
  "dependencies": {
    "@lark-opdev/block-basekit-server-api": "^1.0.4-alpha.25"
  },
  "monorepo": {
    "rootScripts": false
  }
}
